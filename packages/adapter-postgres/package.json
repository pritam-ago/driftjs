{
  "name": "@driftjs/postgres",
  "version": "0.1.0",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "scripts": {
     "build": "tsc -p tsconfig.json",
     "dev": "nodemon --watch src --ext ts --exec \"pnpm run build && node build/index.js\""
  },
  "dependencies": {
    "@driftjs/core": "workspace:^",
    "pg": "^8.12.0"
  },
  
  "devDependencies": {
    "concurrently": "^9.2.1",
    "nodemon": "^3.1.10",
    "@types/pg": "^8.6.6"
  }
}
